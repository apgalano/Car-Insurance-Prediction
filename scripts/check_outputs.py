#!/usr/bin/env python3
"""Script to check the outputs generated by the training pipeline."""

import sys
from pathlib import Path
import pandas as pd

# Add src to path
sys.path.append(str(Path(__file__).parent.parent))

from src.config.settings import OUTPUTS_DIR, FIGURES_DIR, MODELS_DIR, REPORTS_DIR


def check_outputs():
    """Check if all expected outputs were generated."""
    print("🔍 Checking Training Pipeline Outputs")
    print("=" * 40)
    
    # Check directories
    directories = {
        "Figures": FIGURES_DIR,
        "Models": MODELS_DIR,
        "Reports": REPORTS_DIR
    }
    
    for name, directory in directories.items():
        if directory.exists():
            files = list(directory.glob("*"))
            print(f"✅ {name} directory: {len(files)} files")
            for file in files[:5]:  # Show first 5 files
                print(f"   📄 {file.name}")
            if len(files) > 5:
                print(f"   ... and {len(files) - 5} more files")
        else:
            print(f"❌ {name} directory: Not found")
        print()
    
    # Check specific files
    expected_files = {
        "Feature Importance": REPORTS_DIR / "feature_importance.csv",
        "Data Description": REPORTS_DIR / "data_description.csv",
        "Trained Model": MODELS_DIR / "car_insurance_model.pkl"
    }
    
    print("📋 Expected Output Files:")
    for name, filepath in expected_files.items():
        if filepath.exists():
            if filepath.suffix == '.csv':
                try:
                    df = pd.read_csv(filepath)
                    print(f"✅ {name}: {df.shape[0]} rows, {df.shape[1]} columns")
                except Exception as e:
                    print(f"⚠️  {name}: File exists but couldn't read - {e}")
            else:
                size_mb = filepath.stat().st_size / (1024 * 1024)
                print(f"✅ {name}: {size_mb:.2f} MB")
        else:
            print(f"❌ {name}: Not found")
    
    print("\n🎯 Summary:")
    print("If all files show ✅, the training pipeline completed successfully!")
    print("You can now:")
    print("• View plots in outputs/figures/")
    print("• Check feature importance in outputs/reports/feature_importance.csv")
    print("• Load the trained model from outputs/models/car_insurance_model.pkl")


if __name__ == "__main__":
    check_outputs()